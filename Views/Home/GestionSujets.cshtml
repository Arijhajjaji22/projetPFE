@*
    For more information on enabling MVC for empty projects, visit https://go.microsoft.com/fwlink/?LinkID=397860
*@
@{
    Layout = null;
}

<!DOCTYPE html>
@model App_plateforme_de_recurtement.Models.StageOffer

<!doctype html>
<html lang="en">
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <title> Innov-Alliance-Tech </title>

    <!-- Favicon -->
    <link rel="shortcut icon" href="~/images/iat.png" />

    <link rel="stylesheet" href="~/assets/css/backend-plugin.min.css">
    <link rel="stylesheet" href="~/assets/css/backend.css?v=1.0.0">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.4/css/all.min.css">
    <!-- Ajoutez la référence à la bibliothèque DataTables -->
    <link rel="stylesheet" type="text/css" href="https://cdn.datatables.net/v/dt/dt-1.11.5/datatables.min.css" />
</head>
<style>
    .green-button {
        color: green; /* Optionnel: pour changer la couleur du texte à blanc */
    }

    .texttext {
        font-size: 18px;
        color: #333;
        vertical-align: middle;
    }

    .custom-icon img {
        width: 30px; /* ajustez la taille selon vos besoins */
        height: auto; /* permet de conserver les proportions de l'image */
        display: inline-block; /* assure que l'image est alignée avec le texte */
        margin-right: 10px; /* ajustez la marge pour séparer l'image du texte */
    }

    .custom-icone img {
        width: 50px; /* ajustez la taille selon vos besoins */
        height: auto; /* permet de conserver les proportions de l'image */
        display: inline-block; /* assure que l'image est alignée avec le texte */
        margin-right: 10px; /* ajustez la marge pour séparer l'image du texte */
    }

    .role-admin {
        color: red; /* Couleur rouge pour le rôle d'administrateur */
    }

    .role-manager {
        color: blue; /* Couleur bleue pour le rôle de manager */
    }

    .role-rh {
        color: green; /* Couleur verte pour le rôle de R&H */
    }


    .error-input {
        border-color: red !important; /* Couleur de la bordure en rouge */
    }

    .styled-table {
        width: 100%;
        border-collapse: collapse;
        border: 1px solid #ddd;
        font-size: 14px;
    }

        .styled-table th, .styled-table td {
            border: 1px solid #ddd;
            padding: 8px;
            padding: 12px; /* Ajustement du rembourrage */
            text-align: left;
            max-width: 300px;
        }

    .table tbody th,
    .table tbody td {
        font-size: 14px !important; /* Taille de la police */
        color: #333 !important; /* Couleur du texte */
    }

    /* Style pour les cellules de l'en-tête */
    .table thead th {
        font-weight: bold !important; /* Gras */
    }

    .table-color-heading {
        background-color: #021438 !important;
        color: white !important; /* Couleur du texte */
    }

    .styled-table th {
        background-color: #f2f2f2;
        font-weight: bold;
        color: #333;
        text-transform: uppercase;
    }

    .styled-table tr:nth-child(even) {
        background-color: #f9f9f9;
    }

    .styled-table tr:hover {
        background-color: #f2f2f2;
    }
    /* Style pour la couleur de fond de l'en-tête */
    .table-color-heading {
        background-color: white !important;
        color: white !important; /* Couleur du texte */
    }


    /* Style pour les cellules du corps de tableau */
    .table tbody th,
    .table tbody td {
        font-size: 14px !important; /* Taille de la police */
        color: #333 !important; /* Couleur du texte */
    }

    /* Style pour les cellules de l'en-tête */
    .table thead th {
        font-weight: bold !important; /* Gras */
    }


    /* Style pour les actions */
    .actions-header {
        text-align: center !important; /* Alignement centré */
    }

    .modal-dialog {
        max-width: 500px;
        margin: 30px auto;
    }

    .card-title {
        color: #021438; /* Couleur du texte */
        font-size: 29px; /* Taille de la police */
        font-weight: bold; /* Gras */
        margin-bottom: 20px; /* Espacement en bas */
        text-transform: uppercase; /* Convertit le texte en majuscules */
        /* Ombre du texte */
        letter-spacing: 1px; /* Espacement des lettres */
        /* Bordure inférieure */
        padding-bottom: 10px; /* Espacement en bas */
    }

    .icon-color {
        color: #22a6b3 !important; /* Couleur spécifiée */
    }

    .card-title {
        margin-right: 10px; /* Espacement entre l'icône et le texte */
        color: #021438; /* Couleur de l'icône */
    }

    .modal-header i {
        color: #007bff; /* Couleur de l'icône */
        margin-right: 5px; /* Espacement entre l'icône et le texte */
    }

    .modal-title {
        color: #007bff;
    }






    .modal-content {
        background-color: #ffffff;
        border-radius: 10px;
        box-shadow: 0px 0px 20px rgba(0, 0, 0, 0.1);
    }

    .modal-header {
     
        color: #ffffff;
        border-radius: 10px 10px 0 0;
    }

        .modal-header h5 {
            margin: 0;
        }

    .modal-body {
        padding: 30px;
    }

    .form-group {
        margin-bottom: 20px;
    }

        .form-group label {
            font-weight: bold;
            color: #021438;
        }

    .form-control {
        border: 1px solid #cccccc;
        border-radius: 5px;
        padding: 10px;
    }

        .form-control:focus {
            border-color: #021438;
            box-shadow: none;
        }

    .modal-footer {
        border-top: none;
        padding: 20px 30px;
        justify-content: flex-end;
    }

    .btn-primary {
        background-color: #021438;
        border-color: #021438;
        padding: 10px 20px;
    }

        .btn-primary:hover {
            background-color: #023b6e;
            border-color: #023b6e;
        }

    .btn-secondary {
        background-color: #ffffff;
        color: #021438;
        border-color: #021438;
        padding: 10px 20px;
    }

        .btn-secondary:hover {
            background-color: #f0f0f0;
        }

    .close {
        color: #ffffff;
        opacity: 0.7;
    }

        .close:hover {
            opacity: 1;
        }

    * {
        box-sizing: border-box;
    }

    .search-box {
        width: fit-content;
        height: fit-content;
        position: relative;
    }

    .input-search {
        height: 50px;
        width: 50px;
        border-style: none;
        padding: 10px;
        font-size: 18px;
        letter-spacing: 2px;
        outline: none;
        border-radius: 25px;
        transition: all .5s ease-in-out;
        background-color: #22a6b3;
        padding-right: 40px;
        color: #ffffff;
    }

        .input-search::placeholder {
            color: rgba(255,255,255,.5);
            font-size: 18px;
            letter-spacing: 2px;
            font-weight: 100;
        }

    .btn-search {
        width: 50px;
        height: 50px;
        border-style: none;
        font-size: 20px;
        font-weight: bold;
        outline: none;
        cursor: pointer;
        border-radius: 50%;
        position: absolute;
        right: 0px;
        color: #ffffff;
        background-color: transparent;
        pointer-events: painted;
    }

        .btn-search:focus ~ .input-search {
            width: 300px;
            border-radius: 0px;
            background-color: #021438;
            border-bottom: 1px solid rgba(255,255,255,.5);
            transition: all 500ms cubic-bezier(0, 0.110, 0.35, 2);
        }

    .input-search:focus {
        width: 300px;
        border-radius: 0px;
        background-color: #021438;
        border-bottom: 1px solid rgba(255,255,255,.5);
        transition: all 500ms cubic-bezier(0, 0.110, 0.35, 2);
    }

    .edit-btn,
    .delete-btn {
        background-color: #021438; /* Dark blue background */
        color: #fff; /* White text color */
        border: none; /* No border */
        padding: 8px 15px; /* Padding */
        border-radius: 25px; /* Rounded corners */
        cursor: pointer; /* Cursor style */
        font-family: 'Arial', sans-serif; /* Font family */
        font-weight: bold; /* Bold text */
        text-transform: uppercase; /* Uppercase text */
        transition: all 0.3s ease; /* Smooth transition */
    }

        .edit-btn:hover,
        .delete-btn:hover {
            background-color: #22a6b3; /* Orange background on hover */
            transform: scale(1.05); /* Scale up on hover */
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.3); /* Shadow on hover */
        }

        .edit-btn .fas,
        .delete-btn .fas {
            margin-right: 5px; /* Spacing between icon and text */
        }

</style>


<body class="  ">


    <!-- loader Start -->
    <div id="loading">
        <div id="loading-center">
        </div>
    </div>
    <!-- loader END -->
    <!-- Wrapper Start -->
    <div class="wrapper">
        <div class="iq-sidebar  sidebar-default with-background ">
            <div class="iq-sidebar-logo d-flex align-items-end justify-content-between">
                <a href="@Url.Action("ManagerDashboard", "Home")" class="header-logo">
                    <img src="~/images/iat.png" class="img-fluid rounded-normal light-logo" alt="logo">
                    <img src="~/images/iat.png" class="img-fluid rounded-normal d-none sidebar-light-img" alt="logo">
                    <span></span>
                </a>
                <div class="side-menu-bt-sidebar-1">
                    <svg xmlns="http://www.w3.org/2000/svg" class="text-light wrapper-menu" width="30" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12" />
                    </svg>
                </div>
            </div>
            <div class="data-scrollbar" data-scroll="1">
                <nav class="iq-sidebar-menu">
                    <ul id="iq-sidebar-toggle" class="side-menu">
                        <li class="sidebar-layout">
                            <a href="@Url.Action("ManagerDashboard", "Home")">
                                <img src="~/images/menu.gif" alt="Dashboard Image" style="width: 30px; height: 30px;">
                                <span class="ml-2" style="color: #6c757d; font-weight: bold;">Dashboard</span>
                            </a>
                        </li>

                        <li class="px-3 pt-3 pb-2">
                            <span class="text-uppercase small font-weight-bold" style="color: #6c757d;"></span>
                        </li>
                        <li class="sidebar-layout">
                            <a href="@Url.Action("GestionSujets", "Home")">
                                <img src="~/images/task.gif" alt="Gestion Sujets Image" style="width: 30px; height: 30px;">
                                <span class="ml-2" style="color: #6c757d; font-weight: bold;">Gestion des Sujets</span>
                            </a>
                        </li>
                        <li class="px-3 pt-3 pb-2">
                            <span class="text-uppercase small font-weight-bold" style="color: #6c757d;"></span>
                        </li>
                        <li class="sidebar-layout">
                            <a href="@Url.Action("Tests", "Home")">
                                <img src="~/images/test.gif" alt="Dashboard Image" style="width: 30px; height: 30px;">
                                <span class="ml-2" style="color: #6c757d; font-weight: bold;">gestion des tests</span>
                            </a>
                        </li>
                        <li class="px-3 pt-3 pb-2">
                            <span class="text-uppercase small font-weight-bold" style="color: #6c757d;"></span>
                        </li>
                        <li class="sidebar-layout">
                            <a href="@Url.Action("tablesujets", "Home")">
                                <img src="~/images/test.gif" alt="Dashboard Image" style="width: 30px; height: 30px;">
                                <span class="ml-2" style="color: #6c757d; font-weight: bold;">SUJETS APPROUVÉS</span>
                            </a>
                        </li>
                    </ul>
                </nav>

                <div class="pt-5 pb-5"></div>
            </div>


        </div>
        <div class="iq-top-navbar">
            <div class="iq-navbar-custom">
                <nav class="navbar navbar-expand-lg navbar-light p-0">
                    <div class="side-menu-bt-sidebar">
                        <svg xmlns="http://www.w3.org/2000/svg" class="text-secondary wrapper-menu" width="30" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h16" />
                        </svg>
                    </div>
                    <div class="d-flex align-items-center">
                        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-label="Toggle navigation">
                            <svg xmlns="http://www.w3.org/2000/svg" class="text-secondary" width="30" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16m-7 6h7" />
                            </svg>
                        </button>
                        <div class="collapse navbar-collapse" id="navbarSupportedContent">
                            <ul class="navbar-nav ml-auto navbar-list align-items-center">
                                <li class="nav-item nav-icon dropdown">
                                    <a href="#" class="search-toggle dropdown-toggle" id="notification-dropdown" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                                        <span class="custom-icon" width: 30px;>
                                            <img src="/images/notification.png" alt="Image icône">
                                        </span>
                                        <span class="bg-primary"></span>
                                    </a>
                                    <div class="iq-sub-dropdown dropdown-menu" aria-labelledby="notification-dropdown">
                                        <div class="card shadow-none m-0 border-0">
                                            <div class="p-3 card-header-border">
                                                <h6 class="text-center">
                                                    Notifications
                                                </h6>
                                            </div>
                                            <div class="card-body overflow-auto card-header-border p-0 card-body-list" style="max-height: 500px;">
                                                <ul class="dropdown-menu-1 overflow-auto list-style-1 mb-0" id="notificationList">
                                                 
                                                </ul>
                                            </div>
                                            <div class="card-footer text-muted p-3">
                                                <p class="mb-0 text-primary text-center font-weight-bold">Show all notifications</p>
                                            </div>
                                        </div>
                                    </div>
                                </li>

                                <li class="nav-item nav-icon search-content">
                                    <a href="#" class="search-toggle rounded" id="dropdownSearch" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                                        <svg class="svg-icon text-secondary" id="h-suns" height="25" width="25" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M21 21l-6-6m2-5a7 7 0 11-14 0 7 7 0 0114 0z" />
                                        </svg>
                                    </a>
                                    <div class="iq-search-bar iq-sub-dropdown dropdown-menu" aria-labelledby="dropdownSearch">
                                        <form action="#" class="searchbox p-2">
                                            <div class="form-group mb-0 position-relative">
                                                <input type="text" class="text search-input font-size-12" placeholder="type here to search...">
                                                <a href="#" class="search-link">
                                                    <svg xmlns="http://www.w3.org/2000/svg" class="" width="20" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                                                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M21 21l-6-6m2-5a7 7 0 11-14 0 7 7 0 0114 0z" />
                                                    </svg>
                                                </a>
                                            </div>
                                        </form>
                                    </div>
                                </li>
                                <li class="nav-item nav-icon dropdown">
                                    <a href="#" class="nav-item nav-icon dropdown-toggle pr-0 search-toggle" id="dropdownMenuButton" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                                        <img src="~/images/user (4).png" class="img-fluid avatar-rounded" alt="user" height="10">
                                        <span class="mb-0 ml-2 user-name">Arij Hajjaji</span>
                                    </a>
                                    <ul class="dropdown-menu dropdown-menu-right" aria-labelledby="dropdownMenuButton">
                                        <li class="dropdown-item d-flex svg-icon">
                                            <svg class="svg-icon mr-0 text-secondary" id="h-01-p" width="20" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5.121 17.804A13.937 13.937 0 0112 16c2.5 0 4.847.655 6.879 1.804M15 10a3 3 0 11-6 0 3 3 0 016 0zm6 2a9 9 0 11-18 0 9 9 0 0118 0z" />
                                            </svg>
                                            <a href="@Url.Action("GestionSujets", "Home")">My Profile</a>

                                        </li>
                                        <li class="dropdown-item d-flex svg-icon">
                                            <svg class="svg-icon mr-0 text-secondary" id="h-02-p" width="20" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M11 5H6a2 2 0 00-2 2v11a2 2 0 002 2h11a2 2 0 002-2v-5m-1.414-9.414a2 2 0 112.828 2.828L11.828 15H9v-2.828l8.586-8.586z" />
                                            </svg>
                                            <a href="../app/user-profile-edit.html">Edit Profile</a>
                                        </li>
                                        <li class="dropdown-item d-flex svg-icon">
                                            <svg class="svg-icon mr-0 text-secondary" id="h-03-p" width="20" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10.325 4.317c.426-1.756 2.924-1.756 3.35 0a1.724 1.724 0 002.573 1.066c1.543-.94 3.31.826 2.37 2.37a1.724 1.724 0 001.065 2.572c1.756.426 1.756 2.924 0 3.35a1.724 1.724 0 00-1.066 2.573c.94 1.543-.826 3.31-2.37 2.37a1.724 1.724 0 00-2.572 1.065c-.426 1.756-2.924 1.756-3.35 0a1.724 1.724 0 00-2.573-1.066c-1.543.94-3.31-.826-2.37-2.37a1.724 1.724 0 00-1.065-2.572c-1.756-.426-1.756-2.924 0-3.35a1.724 1.724 0 001.066-2.573c-.94-1.543.826-3.31 2.37-2.37.996.608 2.296.07 2.572-1.065z" />
                                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 12a3 3 0 11-6 0 3 3 0 016 0z" />
                                            </svg>
                                            <a href="../app/user-account-setting.html">Account Settings</a>
                                        </li>
                                        <li class="dropdown-item d-flex svg-icon">
                                            <svg class="svg-icon mr-0 text-secondary" id="h-04-p" width="20" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m5.618-4.016A11.955 11.955 0 0112 2.944a11.955 11.955 0 01-8.618 3.04A12.02 12.02 0 003 9c0 5.591 3.824 10.29 9 11.622 5.176-1.332 9-6.03 9-11.622 0-1.042-.133-2.052-.382-3.016z" />
                                            </svg>
                                            <a href="../app/user-privacy-setting.html">Privacy Settings</a>
                                        </li>
                                        <li class="dropdown-item  d-flex svg-icon border-top">
                                            <svg class="svg-icon mr-0 text-secondary" id="h-05-p" width="20" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 16l4-4m0 0l-4-4m4 4H7m6 4v1a3 3 0 01-3 3H6a3 3 0 01-3-3V7a3 3 0 013-3h4a3 3 0 013 3v1" />
                                            </svg>
                                            <a href="@Url.Action("Logout", "Auth", new { returnUrl = "/Home/SeConnecter" })">Logout</a>
                                        </li>
                                    </ul>
                                </li>
                            </ul>
                        </div>
                    </div>
                </nav>
            </div>
        </div>

        <div class="content-page">





            <div class="container-fluid">
                <div class="row">
                    <div class="row">
                        




                    </div>



                    <div class="col-sm-12 col-lg-12">
                        <div class="card">
                            <div class="card-header d-flex justify-content-between">
                                <div class="header-title">
                                    <h4 class="card-title">
                                        <span class="icon-color">
                                            <span class="custom-icone">
                                                <img src="/images/task.gif" alt="Image icône">
                                            </span> Liste des sujets
                                    </h4>
                                </div>
                                <div class="search-box d-flex justify-content-end">
                                    <form id="searchForm">
                                        <!-- Ajout du formulaire -->
                                        <button type="submit" class="btn-search"><i class="fas fa-search"></i></button> <!-- Changement de type en "submit" -->
                                        <input type="text" class="input-search" placeholder="Type to Search...">
                                    </form>
                                </div>
                            </div>
                            <div class="card-body d-flex justify-content-end">
                                <a href="@Url.Action("about", "Home")" class="btn-sm" title="Ajouter une offre de stage" data-bs-toggle="tooltip" data-bs-placement="top">
                                    <span class="custom-icon">
                                        <img src="/images/plus.png" alt="Image icône">
                                    </span>
                                </a>
                            </div>


                            <div class="card-body">
                                <div class="table-responsive">
                                    <table class="table table-bordered table-fluid" id="sujetsTable">
                                        <thead>
                                            <tr>
                                                <th><i class="fas fa-heading icon-color"></i> Titre</th>
                                                <th><i class="fas fa-align-left icon-color"></i> Description</th>
                                                <th><i class="fas fa-industry icon-color"></i> Domaine d'Activité</th>
                                                <th><i class="fas fa-graduation-cap icon-color"></i> Formation</th>
                                                <th><i class="fas fa-check icon-color"></i> Validation</th>

                                                <th><i class="fas fa-cogs icon-color"></i> Actions</th>
                                            </tr>
                                        </thead>
                                        <tbody>
                                            <!-- Les lignes de données de la table iraient ici -->
                                        </tbody>
                                    </table>
                                    <div class="d-flex justify-content-end">
                                        <button id="nextBtn" class="btn btn-sm">
                                            <span class="custom-icon">
                                                <img src="/images/right-chevron.png" alt="Image icône">
                                            </span>
                                        </button>
                                        <button id="prevBtn" class="btn btn-sm" style="display: none;">
                                            <span class="custom-icon">
                                                <img src="/images/left.png" alt="Image icône">
                                            </span>
                                        </button>
                                    </div>
                                </div>
                            </div>


                            <div id="testFormContainer" style="display:none;">
                                <form id="testForm">
                                    <h2>Modifier le Test</h2>
                                    <input type="hidden" id="testId" name="testId">
                                    <label for="testName">Nom du Test:</label>
                                    <input type="text" id="testName" name="testName"><br><br>
                                    <label for="testQuestion">Question:</label>
                                    <textarea id="testQuestion" name="testQuestion" rows="4" cols="50"></textarea><br><br>
                                    <button type="button" onclick="saveChanges()">Enregistrer</button>
                                    <button type="button" onclick="downloadPDF()">Télécharger en PDF</button>
                                    <button type="button" onclick="hideForm()">Annuler</button>
                                </form>
                            </div>
                            <div class="card-body">
                                <div class="collapse" id="form-element-1">
                                    <div class="card">
                                        <kbd class="bg-dark">
                                        </kbd>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>
                    <div class="modal fade" id="offerDetailsDialog" tabindex="-1" aria-labelledby="offerDetailsDialogLabel" aria-hidden="true">
                        <div class="modal-dialog modal-dialog-centered">
                            <div class="modal-content">
                                <div class="modal-header">
                                    <h5 class="modal-title" id="offerDetailsDialogLabel">Détails de l'Offre</h5>
                                    <button type="button" class="btn-close close" data-bs-dismiss="modal" aria-label="Close"> <i class="fas fa-times"></i> </button>
                                   
                                </div>
                                <div class="modal-body" id="offerDetailsBody">
                                    <!-- Les détails de l'offre seront affichés ici -->
                                </div>
                            </div>
                        </div>
                    </div>



                </div>
            </div>
            <!-- Modal -->
            <div class="modal fade" id="editOfferDialog" tabindex="-1" aria-labelledby="editOfferDialogLabel" aria-hidden="true">
                <div class="modal-dialog modal-lg">
                    <div class="modal-content">
                        <div class="modal-header">
                            <h5 class="modal-title" id="editOfferDialogLabel"><i class="fas fa-pencil-alt icon-color"></i> Modifier l'offre</h5>
                            <button type="button" class="close" data-bs-dismiss="modal" aria-label="Close" id="closeEditOfferBtn">
                                <i class="fas fa-times"></i> <!-- Changer l'icône du bouton close -->
                            </button>
                        </div>
                        <div class="modal-body">
                            <!-- Ajout de l'élément input caché pour stocker l'identifiant de l'offre -->
                            <input type="hidden" id="editOfferId" name="editOfferId">
                            <div class="mb-3">
                                <label for="editOfferTitle" class="form-label"><i class="fas fa-heading icon-color"></i> Titre</label>
                                <input type="text" class="form-control" id="editOfferTitle" placeholder="Titre de l'offre">
                            </div>
                            <div class="mb-3">
                                <label for="editOfferDescription" class="form-label"><i class="fas fa-align-left icon-color"></i> Description</label>
                                <textarea class="form-control" id="editOfferDescription" rows="3" placeholder="Description de l'offre"></textarea>
                            </div>
                            <div class="mb-3">
                                <label for="editOfferDomaine" class="form-label"><i class="fas fa-industry icon-color"></i> Domaine d'Activité</label>
                                <input type="text" class="form-control" id="editOfferDomaine" placeholder="Domaine d'activité">
                            </div>
                            <div class="mb-3">
                                <label for="editOfferCompetences" class="form-label"><i class="fas fa-tasks icon-color"></i> Compétences Requises</label>
                                <input type="text" class="form-control" id="editOfferCompetences" placeholder="Compétences requises">
                            </div>
                            <div class="row">
                                <div class="col-md-6 mb-3">
                                    <label for="editOfferDateDebut" class="form-label"><i class="fas fa-calendar-day icon-color"></i> Date de Début</label>
                                    <input type="text" class="form-control" id="editOfferDateDebut">
                                </div>
                                <div class="col-md-6 mb-3">
                                    <label for="editOfferDateFin" class="form-label"><i class="fas fa-calendar-day icon-color"></i> Date de Fin</label>
                                    <input type="text" class="form-control" id="editOfferDateFin">
                                </div>
                            </div>
                            <div class="mb-3">
                                <label for="editOfferNiveauEtudes" class="form-label"><i class="fas fa-graduation-cap icon-color"></i> Niveau d'Études Requis</label>
                                <input type="text" class="form-control" id="editOfferNiveauEtudes" placeholder="Niveau d'études requis">
                            </div>
                            <div class="mb-3">
                                <label for="editOfferTypeStage" class="form-label"><i class="fas fa-location-arrow icon-color"></i> Type de Stage</label>
                                <input type="text" class="form-control" id="editOfferTypeStage" placeholder="Type de stage">
                            </div>
                        </div>
                        <div class="modal-footer">
                            <button type="button" class="btn" data-bs-dismiss="modal" id="cancelEditOfferBtn"> <span class="custom-icon"><img src="/images/delete (1).png" alt="Image icône"></span></button>
                            <button type="button" class="btn " id="saveEditOfferBtn"> <span class="custom-icon"><img src="/images/circle (1).png" alt="Image icône"></span> </button>
                        </div>
                    </div>
                </div>
            </div>


        </div>
        <!-- Dialogue de confirmation de suppression -->
        <!-- Ajoutez ce code HTML dans votre page pour le dialogue de confirmation -->
        <!-- Modal de confirmation de suppression -->
        <div class="modal fade custom-modal" id="confirmArchiveDialog" tabindex="-1" aria-labelledby="deleteConfirmationModalLabel" aria-hidden="true">
            <div class="modal-dialog modal-dialog-centered modal-lg">
                <div class="modal-content">
                    <div class="modal-header">
                        <h5 class="modal-title" id="deleteConfirmationModalLabel"><i class="fas fa-exclamation-circle"></i> Confirmation de suppression</h5>
                        <button type="button" class="btn-close custom-close-btn close" id="closeDialogBtn" aria-label="Close"><i class="fas fa-times"></i></button>
                    </div>
                    <div class="modal-body">
                       
                        <p>Êtes-vous sûr de vouloir supprimer cette offre de stage ?</p>
                    </div>
                    <div class="modal-footer">
                        <button type="button" class=" btn" id="cancelDeleteBtn" data-bs-dismiss="modal"> <span class="custom-icon"><img src="/images/delete (1).png" alt="Image icône"></span> </button>
                        <button type="button" class="btn" id="confirmDeleteBtn"> <span class="custom-icon"><img src="/images/circle (1).png" alt="Image icône"></span></button>
                    </div>
                </div>
            </div>
        </div>
        <div class="modal fade custom-modal" id="confirmActivatedDialog" tabindex="-1" aria-labelledby="deleteConfirmationModalLabel" aria-hidden="true">
            <div class="modal-dialog modal-dialog-centered modal-lg">
                <div class="modal-content">
                    <div class="modal-header">
                        <h5 class="modal-title" id="deleteConfirmationModalLabel"><i class="fas fa-exclamation-circle"></i> Confirmation de validation</h5>
                        <button type="button" class="btn-close custom-close-btn close" id="closeActivatedBtn" aria-label="Close"><i class="fas fa-times"></i></button>
                    </div>
                    <div class="modal-body">

                        <p>Êtes-vous sûr de vouloir valider ce sujet ?</p>

                    </div>
                    <div class="modal-footer">
                        <div class="modal-footer">
                            <button type="button" class="btn btn-cancel" data-bs-dismiss="modal">
                                <span class="custom-icon"><img src="/images/delete (1).png" alt="Image icône"></span>
                            </button>
                            <button type="button" class="btn btn-confirm">
                                <span class="custom-icon"><img src="/images/circle (1).png" alt="Image icône"></span>
                            </button>
                        </div>


                    </div>
                </div>
            </div>
        </div>



    </div>
    <!-- Wrapper End-->
    <footer class="iq-footer">
        <div class="container-fluid">
            <div class="row">
                <div class="col-lg-6">
                    <ul class="list-inline mb-0">
                        <li class="list-inline-item"><a href="../backend/privacy-policy.html">Privacy Policy</a></li>
                        <li class="list-inline-item"><a href="../backend/terms-of-service.html">Terms of Use</a></li>
                    </ul>
                </div>
                <div class="col-lg-6 text-right">
                    <span class="mr-1">
                        Copyright
                        <script>document.write(new Date().getFullYear())</script>© <a href="#" class="">Innov-Alliance-Tech</a>
                        All Rights Reserved.
                    </span>
                </div>
            </div>
        </div>
        <!-- Ajoutez la référence à la bibliothèque DataTables -->
    
    </footer>    <!-- Backend Bundle JavaScript -->
    <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
    <script src="https://cdn.datatables.net/v/dt/dt-1.11.5/datatables.min.js"></script>

   
   

    <script>

        var currentPage = 1;
        var rowsPerPage = 5;

        $(document).ready(function () {
            $('#sujetsTable tbody tr').each(function () {
                var button = $(this).find('.btn-validation');
                var subjectId = button.attr('onclick').match(/'([^']+)'/)[1]; // Extraire l'ID du sujet depuis l'attribut onclick

                if (localStorage.getItem('validated-' + subjectId) === 'true') {
                    button.addClass('green-button');
                    var icon = button.find('i');
                    icon.removeClass('fa-toggle-off').addClass('fa-toggle-on');
                }
            });
           

            // Appel de la fonction pour charger les premières lignes
            updateSujetsTable(currentPage, rowsPerPage);

            // Écouter le clic sur le bouton "Next"
            $('#nextBtn').click(function () {
                currentPage++;
                updateSujetsTable(currentPage, rowsPerPage);
            });

            // Écouter le clic sur le bouton "Previous"
            $('#prevBtn').click(function () {
                if (currentPage > 1) {
                    currentPage--;
                    updateSujetsTable(currentPage, rowsPerPage);
                }
            });
        });

            $('#closeActivatedBtn').on('click', function () {
                console.log("Clic sur le bouton de fermeture");
                $('#confirmActivatedDialog').modal('hide'); // Fermer le dialogue modal
            });
            // Appel de la fonction pour mettre à jour la table des sujets
        updateSujetsTable();   



            // Écouter le clic sur le bouton de recherche
            $('.btn-search').click(function () {
                var searchValue = $('.input-search').val().toLowerCase(); // Récupérer la valeur de l'entrée de recherche en minuscules
                filterTable(searchValue); // Appeler la fonction pour filtrer la table
            });
        

        function filterTable(value) {
            $('#sujetsTable tbody tr').filter(function () {
                $(this).toggle($(this).text().toLowerCase().indexOf(value) > -1); // Masquer ou afficher les lignes de la table en fonction de la correspondance de la recherche
            });
        }
        // Supposez que ce code soit placé dans une fonction appelée après la validation des sujets
        function fetchValidatedSubjects() {
            $.ajax({
                type: 'GET',
                url: '/api/StageOffer/validated',
                success: function (validatedSubjects) {
                    console.log('Sujets validés récupérés :', validatedSubjects);
                    displayValidatedSubjects(validatedSubjects);
                },
                error: function (error) {
                    console.error('Erreur lors de la récupération des sujets validés :', error);
                }
            });
        }



        $('#offerDetailsDialog .btn-close').click(function () {
            $('#offerDetailsDialog').modal('hide'); // Cacher le dialogue modal
        });
        // Gérer le clic sur le bouton "Fermer" dans le dialogue d'édition
        $('#closeEditOfferBtn').on('click', function () {
            $('#editOfferDialog').modal('hide');
        });
        $('#closeActivatedOfferBtn').on('click', function () {
            $('#editOfferDialog').modal('hide');
        });
        // Gérer le clic sur le bouton "Annuler" dans le dialogue d'édition
        $('#cancelEditOfferBtn').on('click', function () {
            // Effacer les champs du formulaire
            $('#editOfferTitle').val('');
            $('#editOfferDescription').val('');
            $('#editOfferDomaine').val('');
            $('#editOfferCompetences').val('');
            $('#editOfferDateDebut').val('');
            $('#editOfferDateFin').val('');
            $('#editOfferNiveauEtudes').val('');
            $('#editOfferTypeStage').val('');

            // Cacher le dialogue d'édition
            $('#editOfferDialog').modal('hide');
        });

        function toggleActivation(button, subjectId) {
            var icon = button.querySelector('i');

            // Vérifier l'état actuel du sujet (activé ou désactivé)
            var isActivated = icon.classList.contains('fa-toggle-on');

            // Afficher le dialogue de confirmation uniquement si le sujet est désactivé
            if (!isActivated) {
                console.log("Affichage du dialogue de confirmation");
                $('#confirmActivatedDialog').modal('show');

                // Gérer l'événement de clic sur le bouton de confirmation
                $('#confirmActivatedDialog').find('.btn-confirm').off('click').on('click', function () {
                    // Valider le sujet en appelant l'API côté serveur
                    console.log("Clic sur le bouton de confirmation");
                    $.ajax({
                        type: 'POST',
                        url: '/api/StageOffer/validate/' + subjectId, // Appeler l'API pour valider le sujet avec son ID
                        success: function () {
                            // Mettre à jour l'affichage ou effectuer d'autres actions nécessaires après la validation réussie
                            console.log('Sujet validé avec succès');

                            // Changer l'icône du bouton pour refléter l'activation
                            icon.classList.remove('fa-toggle-off');
                            icon.classList.add('fa-toggle-on');

                            // Changer la couleur du bouton en vert
                            $(button).addClass('green-button');

                            // Enregistrer l'état dans le local storage
                            localStorage.setItem('validated-' + subjectId, 'true');
                        },
                        error: function (error) {
                            console.error('Erreur lors de la validation du sujet:', error);
                        }
                    });

                    // Après la validation réussie, masquer le dialogue de confirmation
                    $('#confirmActivatedDialog').modal('hide');
                });

                // Gérer l'événement de clic sur le bouton d'annulation
                $('#confirmActivatedDialog').find('.btn-cancel').off('click').on('click', function () {
                    // Masquer le dialogue de confirmation sans rien faire
                    console.log("Clic sur le bouton d'annulation");
                    $('#confirmActivatedDialog').modal('hide');
                });
            }
        }









        function updateSujetsTable(currentPage, rowsPerPage) {
            // Calculer startIndex et endIndex en fonction de currentPage et rowsPerPage
            var startIndex = (currentPage - 1) * rowsPerPage;
            var endIndex = startIndex + rowsPerPage;

            $.ajax({
                type: 'GET',
                url: '/api/StageOffer/notdeleted',
                success: function (offers) {
                    $('#sujetsTable tbody').empty();
                    offers.slice(startIndex, endIndex).forEach(function (offer) {
                        var rowData = JSON.stringify(offer);
                        // Formater les dates pour afficher uniquement la date
                        var dateDebut = new Date(offer.dateDebut).toLocaleDateString();
                        var dateFin = new Date(offer.dateFin).toLocaleDateString();

                        // Vérifier si competencesRequises est un tableau
                        var competences = Array.isArray(offer.competencesRequises) ? offer.competencesRequises.join(', ') : offer.competencesRequises;

                        // Ajout des boutons Edit, Delete et Details dans une div avec la classe "btn-group"
                        var deleteButton = '<button class="btn btn-sm btn-delete d-inline-block text-nowrap" data-id="' + offer.id + '" data-bs-toggle="modal" data-bs-target="#confirmArchiveDialog" title="Supprimer l\'offre de stage"><span class="custom-icon"><img src="/images/delete (2).png" alt="Image icône"></span></button>';

                        var editButton = '<button class="btn btn-sm btn-edit d-inline-block text-nowrap" data-id="' + offer.id + '" title="Modifier l\'offre de stage"><span class="custom-icon"><img src="/images/pen (1).png" alt="Image icône"></span></button>';

                        var detailsButton = '<button class="btn btn-sm btn-details d-inline-block text-nowrap" data-offer=\'' + rowData + '\' title="Voir plus de détails"><span class="custom-icon"><img src="/images/more.png" alt="Image icône"></span></button>';

                        // Ajout du bouton "Afficher le test"
                        var viewTestButton = '<button class="btn btn-sm btn-view-test" onclick="viewTest(\'' + offer.id + '\')" title="Afficher le test"><span class="custom-icon"><img src="/images/homework.png" alt="Image icône"></span></button>';

                        var buttonsDiv = '<div class="btn-group">' + editButton + deleteButton + detailsButton + viewTestButton + '</div>';

                        $('#sujetsTable tbody').append('<tr>' +
                            '<td>' + offer.titre + '</td>' +
                            '<td>' + offer.description + '</td>' +
                            '<td>' + offer.domaineActivite + '</td>' +
                            '<td>' + offer.niveauEtudesRequis + '</td>' +
                            '<td><button class="btn btn-sm btn-validation" onclick="toggleActivation(this, \'' + offer.id + '\')"><i class="fas fa-toggle-' + (offer.valide ? 'on' : 'off') + ' fa-lg"></i></button></td>' +
                            '<td>' + buttonsDiv + '</td>' + // Ajout des boutons à cette cellule
                            '</tr>');
                    });

                    // Afficher ou masquer le bouton "Previous" en fonction de la page actuelle
                    if (currentPage > 1) {
                        $('#prevBtn').show();
                    } else {
                        $('#prevBtn').hide();
                    }

                    // Afficher ou masquer le bouton "Next" en fonction du nombre total d'offres et de la page actuelle
                    if (endIndex < offers.length) {
                        $('#nextBtn').show();
                    } else {
                        $('#nextBtn').hide();
                    }
                },
                error: function (error) {
                    console.error('Erreur lors de la récupération des offres de stage :', error);
                }
            });
        }
        function viewTest(offerId) {
            // Envoyez une requête AJAX pour récupérer le test associé à l'offre de stage
            $.ajax({
                type: 'GET',
                url: '/api/Test/' + offerId, // Modifier l'URL pour inclure l'identifiant de l'offre de stage comme sujet
                success: function (tests) {
                    // Affichez les tests récupérés dans la console pour vérification
                    console.log('Tests récupérés avec succès :', tests);
                    // Stocker les tests récupérés dans le Local Storage
                    localStorage.setItem('tests', JSON.stringify(tests));
                    // Rediriger vers la page Testform avec l'identifiant de l'offre de stage
                   window.location.href = '@Url.Action("Testform", "Home")' + '?offerId=' + offerId;
                },
                error: function (error) {
                    console.error('Erreur lors de la récupération des tests :', error);
                }
            });
        }


















        // Gérer le clic sur le bouton "More"
        $('#sujetsTable').on('click', '.btn-details', function () {
            // Récupérer les données de l'offre associées à la ligne sur laquelle vous avez cliqué
            var offerData = $(this).data('offer');
            var offerDetails = '<table class="table table-bordered">'; // Début de la table pour afficher les détails

            // Parcourir chaque propriété de l'offre pour obtenir les détails complets
            for (var key in offerData) {
                if (offerData.hasOwnProperty(key) && !key.includes('SansHeure') && key !== 'valide' && key !== 'isDeleted' && key !== 'approved' && key !== 'desapprouve') {
                    // Exclure les propriétés contenant 'SansHeure', 'valide' et 'isDeleted'
                    // Vérifier si la propriété est une date
                    if (key === 'dateDebut' || key === 'dateFin') {
                        // Formater la date pour afficher uniquement la partie date
                        var date = new Date(offerData[key]).toLocaleDateString();
                        offerDetails += '<tr><td><strong>' + key + ':</strong></td><td>' + date + '</td></tr>'; // Ajoutez chaque propriété comme une ligne dans la table
                    } else {
                        offerDetails += '<tr><td><strong>' + key + ':</strong></td><td>' + offerData[key] + '</td></tr>'; // Ajoutez chaque propriété comme une ligne dans la table
                    }
                }
            }

            offerDetails += '</table>'; // Fin de la table

            // Afficher les détails complets dans le dialogue modal
            $('#offerDetailsBody').html(offerDetails);
            $('#offerDetailsDialog').modal('show');
        });









        // Gérer le clic sur le bouton "Edit"
        $('#sujetsTable').on('click', '.btn-edit', function () {
            // Récupérer les données de la ligne sélectionnée dans le tableau
            var row = $(this).closest('tr');
            var offerTitle = row.find('td:eq(0)').text().trim(); // Titre de l'offre
            var offerDescription = row.find('td:eq(1)').text().trim(); // Description de l'offre
            var offerDomaine = row.find('td:eq(2)').text().trim(); // Domaine d'activité
            var offerCompetences = row.find('td:eq(3)').text().trim(); // Compétences requises
            var offerDateDebut = row.find('td:eq(4)').text().trim(); // Date de début
            var offerDateFin = row.find('td:eq(5)').text().trim(); // Date de fin
            var offerNiveauEtudes = row.find('td:eq(6)').text().trim(); // Niveau d'études requis
            var offerTypeStage = row.find('td:eq(7)').text().trim(); // Type de stage
            var offerId = $(this).data('id'); // Récupérer l'identifiant de l'offre

            // Mettre à jour le champ d'identifiant dans le formulaire de modification
            $('#editOfferId').val(offerId);

            // Convertir les valeurs de date en format adapté pour les champs de date HTML
            var formattedDateDebut = offerDateDebut.split('/').reverse().join('-');
            var formattedDateFin = offerDateFin.split('/').reverse().join('-');

            // Remplir les champs du formulaire de modification avec les données récupérées
            $('#editOfferTitle').val(offerTitle);
            $('#editOfferDescription').val(offerDescription);
            $('#editOfferDomaine').val(offerDomaine);
            $('#editOfferCompetences').val(offerCompetences);
            $('#editOfferDateDebut').val(formattedDateDebut);
            $('#editOfferDateFin').val(formattedDateFin);
            $('#editOfferNiveauEtudes').val(offerNiveauEtudes);
            $('#editOfferTypeStage').val(offerTypeStage);

            // Afficher le dialogue de modification
            $('#editOfferDialog').modal('show');
        });


  
        // Gérer le clic sur le bouton "Save" dans le dialogue d'édition
        // Gérer la sauvegarde des modifications
        // Gérer le clic sur le bouton "Save" dans le dialogue d'édition
        $('#saveEditOfferBtn').on('click', function () {
            // Récupérer les valeurs modifiées des champs dans le dialogue de modification
            var id = $('#editOfferId').val(); // Si vous avez un champ caché pour stocker l'ID de l'offre
            var title = $('#editOfferTitle').val();
            var description = $('#editOfferDescription').val();
            var domaine = $('#editOfferDomaine').val();
            var competences = $('#editOfferCompetences').val();
            var dateDebut = $('#editOfferDateDebut').val();
            var dateFin = $('#editOfferDateFin').val();
            var niveauEtudes = $('#editOfferNiveauEtudes').val();
            var typeStage = $('#editOfferTypeStage').val();

            // Créer un objet contenant les valeurs modifiées
            var newData = {
                Id: id,
                Titre: title,
                Description: description,
                DomaineActivite: domaine,
                CompetencesRequises: competences,
                DateDebut: dateDebut,
                DateFin: dateFin,
                NiveauEtudesRequis: niveauEtudes,
                TypedeStage: typeStage
            };


            // Envoyer les données modifiées au serveur via une requête AJAX PUT
            $.ajax({
                type: 'PUT',
                url: '/api/StageOffer/' + id,
                contentType: 'application/json',
                data: JSON.stringify(newData),
                success: function () {
                    // Mettre à jour l'affichage pour refléter les modifications
                    updateSujetsTable();
                    console.log('Offre de stage modifiée avec succès');
                },
                error: function (error) {
                    console.error('Erreur lors de la modification de l\'offre de stage:', error);
                }
            });

            // Fermer la fenêtre modale de modification
            $('#editOfferDialog').modal('hide');
        });



        // Utilisation de la délégation d'événements pour le clic sur le bouton "Delete"
        $('#sujetsTable').on('click', '.btn-delete', function () {
            var offerId = $(this).data('id');
            $('#confirmArchiveDialog').modal('show');
            // Gérer la confirmation de la suppression
            $('#confirmDeleteBtn').off().on('click', function () {
                $.ajax({
                    type: 'DELETE',
                    url: '/api/StageOffer/' + offerId,
                    success: function () {
                        updateSujetsTable(currentPage, rowsPerPage); // Correction de la fonction appelée ici
                        console.log('Offre de stage supprimée avec succès');
                    },
                    error: function (error) {
                        console.error('Erreur lors de la suppression de l\'offre de stage:', error);
                    }
                });
                $('#confirmArchiveDialog').modal('hide');
            });

            // Gérer l'annulation de la suppression
            $('#cancelDeleteBtn').off().on('click', function () {
                $('#confirmArchiveDialog').modal('hide');
                updateSujetsTable(currentPage, rowsPerPage);
            });
        });

        // Gérer le clic sur le bouton de fermeture de la modale
        $('#closeDialogBtn').on('click', function () {
            $('#confirmArchiveDialog').modal('hide');
        });

        // Empêcher la soumission du formulaire lorsque vous appuyez sur "Entrée"
        $('#searchForm').on('submit', function (e) {
            e.preventDefault();
            var searchValue = $('.input-search').val().toLowerCase(); // Récupérer la valeur de l'entrée de recherche en minuscules
            filterTable(searchValue); // Appeler la fonction pour filtrer la table
        });
        // Cette fonction récupère les notifications de l'API et les affiche dans l'interface administrateur



    </script>

    <script src="~/assets/js/backend-bundle.min.js"></script>
    <!-- Chart Custom JavaScript -->
    <script src="~/assets/js/customizer.js"></script>

    <script src="~/assets/js/sidebar.js"></script>

    <!-- Flextree Javascript-->
    <script src="~/assets/js/flex-tree.min.js"></script>
    <script src="~/assets/js/tree.js"></script>

    <!-- Table Treeview JavaScript -->
    <script src="~/assets/js/table-treeview.js"></script>

    <!-- SweetAlert JavaScript -->
    <script src="~/assets/js/sweetalert.js"></script>

    <!-- Vectoe Map JavaScript -->
    <script src="~/assets/js/vector-map-custom.js"></script>

    <!-- Chart Custom JavaScript -->
    <script src="~/assets/js/chart-custom.js"></script>
    <script src="~/assets/js/charts/01.js"></script>
    <script src="~/assets/js/charts/02.js"></script>

    <!-- slider JavaScript -->
    <script src="~/assets/js/slider.js"></script>

    <!-- Emoji picker -->
    <script src="~/assets/vendor/emoji-picker-element/index.js" type="module"></script>


    <!-- app JavaScript -->
    <script src="~/assets/js/app.js"></script>
</body>
</html>
